apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: guacamole-config
  name: guacamole-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
