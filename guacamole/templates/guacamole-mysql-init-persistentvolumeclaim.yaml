apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: guacamole-mysql-init
  name: guacamole-mysql-init
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
